#!/bin/bash

DIRNAME=$(cd `dirname $0` && pwd)
USERNAME=$(<~/.meteor/wishpool-widget.com.username)
PASSWORD=$(<~/.meteor/wishpool-widget.com.password)
HOST="proximus.modulusmongo.net"
PORT="27017"
DB="Y2petuqe"
OUT_DIR="/tmp"

mongo -u $USERNAME -p $PASSWORD $HOST:$PORT/$DB
